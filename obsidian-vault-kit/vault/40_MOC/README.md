# 40_MOC — 地図ノート（Map of Content）

あるテーマの**入口**。ノートが増えて辿れなくなったときに作る。

- リンクの羅列にしない。並び順と一言解説に思考を出す。
- `## 現時点の立場` が最重要。ノート群を読んだ上での総合を3行で書く。
- `## 空白` に「まだ考えていない論点」を残す。次に書くノートの候補になる。

対象ノートが3つ未満なら、まだ MOC を作る段階ではない。
`moc-builder` スキルで作成・更新する。
